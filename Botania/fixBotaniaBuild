#bin/bash

#This scrpit is only used to delete and replace the unnescessary Ant gralde system made by vazkii.
#You can add your own download URL for your custom build.gradle file
blue='\e[1;34m'
red='\e[1;31m'
newcol='\e[0m'
URL=''

#Start Makeing the build sane for maven and CI servers
echo -e "${blue}Now fixing Botaina's build process to make it sane for maven users${clear}"

#kill unneeded files
echo -e "${red}Removeing all unnecessary files and direcories${clear}"
rm -r -v web eclispe version ftp.eclipse.example *.bat

#TEMP TILL I MAKE A MIRROR FOR BAUBLES
mkdir libs
wget https://dl.dropboxusercontent.com/u/47135879/Baubles-deobf-1.7.10-1.0.1.8.jar

#Replace Shitty Ant+Grale with a Gradle proper gradle scipt

